import { describe, it, expect } from 'vitest';

describe('scaffold', () => {
  /**
   * Minimal assertion that proves the Vitest test runner resolves
   * globals (describe, it, expect) and exits cleanly.
   */
  it('confirms the test infrastructure works', () => {
    expect(1 + 1).toBe(2);
  });
});
