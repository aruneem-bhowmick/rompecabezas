describe('scaffold', () => {
  /**
   * Minimal assertion that exercises the global test APIs (describe,
   * it, expect) provided by the Vitest configuration and confirms
   * the runner exits cleanly.
   */
  it('confirms the test infrastructure works', () => {
    expect(1 + 1).toBe(2);
  });
});
