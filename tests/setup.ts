/**
 * Vitest global setup file.
 *
 * Registers the extended DOM matchers provided by
 * @testing-library/jest-dom (e.g. toBeInTheDocument, toHaveClass)
 * so they are available in every test without per-file imports.
 */
import '@testing-library/jest-dom';
